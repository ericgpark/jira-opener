// Below is an example file, which will demonstrate the JIRA Opener extension's functionality.

export function sum(a, b) {
  // To-do: Output 'Hello World' to the console (ABC-123)
  return a + b;
};
// To-do: Add more functions to this file (XYZ-456)
export function multiply(a, b) {
  return a * b;
}
// To-do: Refactor the sum function to handle more cases (DEF-789)
export function sumArray(arr) {
  return arr.reduce((acc, val) => acc + val, 0);
}